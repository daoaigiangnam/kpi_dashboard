@extends('layouts.admin') @section('title',$group->exists?'Edit Group':'New Group') @section('content')<div class="card form"><form method="post" action="{{ $group->exists?route('admin.groups.update',$group):route('admin.groups.store') }}">@csrf @if($group->exists) @method('PUT') @endif<div class="field"><label>Name</label><input class="input" name="name" value="{{ old('name',$group->name) }}" required></div><div class="field"><label>Description</label><input class="input" name="description" value="{{ old('description',$group->description) }}"></div><h3>Permissions</h3>@foreach($permissions as $module=>$items)<div class="card" style="margin:10px 0"><strong>{{ $module }}</strong>@foreach($items as $p)<div class="check"><label><input type="checkbox" name="permissions[]" value="{{ $p->id }}" @checked(in_array($p->id,old('permissions',$group->permissions->pluck('id')->all())))> {{ $p->name }} <span class="muted">({{ $p->code }})</span></label></div>@endforeach</div>@endforeach<button class="btn">Save</button> <a class="btn gray" href="{{ route('admin.groups.index') }}">Cancel</a></form></div>@endsection
